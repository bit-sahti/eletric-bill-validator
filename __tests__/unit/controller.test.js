const { elegibilityController } = require('../../src/controller')

describe('Elegibility Controller test suite', () => {
  it.todo('should get the average consumption and pass it to the service')
  it.todo(
    'should get and return the CO2 economy projection if client is eligible'
  )
  it.todo(
    'should get and return the ineligibility reasons if client is ineligible'
  )
})
